import java.util.Scanner;

public class Towers {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
     Scanner sc = new Scanner(System.in);
     while(true){
    	float towerSignal1 = sc.nextFloat();
    	float towerSignal2 = sc.nextFloat();
    	float towerSignal3 = sc.nextFloat();
    	
    	int d1 = Math.sqrt(Math.pow(-100+x,2) + Math.pow(-100+y,2));
    	int d2 = Math.sqrt(Math.pow(0+x,2) + Math.pow(100+y,2));
    	int d3 = Math.sqrt(Math.pow(100+x,2) + Math.pow(-100+y,2));
    	
    	//float p1 = s*(towerSignal1 * );
    	
    	 
     }
	}

}
